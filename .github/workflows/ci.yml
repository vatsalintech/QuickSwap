name: Go CI (Deprecated)

on:
  push:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow has been replaced by backend-ci.yml and frontend-ci.yml. Delete this file later."
